{
    "name": "app",
    "scripts": {
        "dev": "$npm_execpath run build && $npm_execpath run start",
        "build": "tsc index.ts --outDir dist",
        "start": "node dist/index.js"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": "^20.11.0",
        "yarn": "^4.0.2"
    },
    "packageManager": "yarn@4.0.2"
}
